@model IEnumerable<BabunBank.Models.User.UserViewModel>

@{
    ViewBag.Title = "User index page";
    Layout = "_Layout";
}

<h2>User index page</h2>
@foreach (var user in Model)
{
    <h3>@user.UserName @user.Email</h3>
}